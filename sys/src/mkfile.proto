#
# common mkfile parameters shared by all architectures
#

OS=56789qv	#12k
CPUS=amd64 arm 386
#CPUS=arm alpha 386 amd64 power mips
CFLAGS=-FTVw
LEX=lex
YACC=yacc
MK=/bin/mk

# recursive mk will have these set from the parent
# this is never what we want.  clear them

TARG=
OFILES=
HFILES=
YFILES=
